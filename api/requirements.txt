# Vercel API Requirements - Companion AI Framework
# FastAPI-based unified API server dependencies

# Core FastAPI and async support
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# CORS support
python-multipart==0.0.6

# Database - SQLite only (no compilation needed)
sqlalchemy==2.0.20

# Authentication and security
PyJWT==2.8.0
bcrypt==4.0.1
cryptography==41.0.7

# HTTP client
httpx==0.25.2
aiohttp==3.9.0

# Environment and configuration
python-dotenv==1.0.0

# Logging and utilities
python-json-logger==2.0.7

# Data processing - numpy now optional (handled in code)
# numpy==1.24.4

# Optional AI/ML dependencies (may not be available on Vercel free tier)
# sentence-transformers==2.2.2
# elasticsearch==8.11.0
# meilisearch==0.28.3
# soundfile==0.12.1
# PyPDF2==3.0.1