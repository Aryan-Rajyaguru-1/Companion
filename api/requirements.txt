# Vercel API Requirements - Companion AI Framework
# Minimal Flask application dependencies (Vercel-compatible)

flask==2.3.3
flask-cors==4.0.0
requests==2.31.0
python-dotenv==1.0.0

# Database - SQLite only (no compilation needed)
sqlalchemy==2.0.20
flask-sqlalchemy==3.0.5

# Authentication
PyJWT==2.8.0
bcrypt==4.0.1
werkzeug==2.3.7

# Async support (lightweight)
aiohttp==3.9.0

# Logging
python-json-logger==2.0.7